# 📝 版本变更记录 (Changelog)

本项目遵循 [Semantic Versioning (语义化版本)](https://semver.org/lang/zh-CN/) 规范，记录格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [1.7.3] - 2026-09-17

### 🌟 核心亮点与组件工程化解耦重构 (Architecture & Component Decoupling)
- **超大单文件组件全面解耦与结构化降维 (Large Component Decoupling)**：
  - 针对原工程中代码行数达 1600~2700 行的 5 大单文件组件展开深度解耦，坚持**「样式紧随组件（内聚 scoped style）」**原则，消除视图混杂、海量样式堆叠与维护黑洞：
    - **`RuleTutorial.vue` 骤减 1264 行（2688 -> 1424 行，降幅达 47%）**：抽取独立子组件 `src/components/rules/RuleSplitPrinciples.vue`，内聚四大原则图解、避坑大PK对决卡片、闯关答题 Quiz 与微观拆字实验室；
    - **`ArticlePractice.vue` 骤减 673 行（1809 -> 1136 行，降幅达 37%）**：抽取长文题库大全 `ArticleLibraryModal.vue`、文件拖拽/粘贴导入 `ArticleCustomImportModal.vue` 与长文篇章战报 `ArticleFinishModal.vue`；
    - **`TypeEngine.vue` 净减 289 行（1625 -> 1336 行）**：抽取双层练习控制条 `TypeEngineToolbar.vue`（分类/词组筛选/出字模式/组量/米字格与辅助提示）与分批练习战报结算 `TypeResultModal.vue`；
    - **`TypingChaseGame.vue` 净减 192 行（1721 -> 1529 行）**：抽取赛车结算面板 `ChaseResultModal.vue`，并沉淀专用追逐赛输入调度引擎 `useChaseInput.ts`；
    - **`RpgAdventure.vue` 净减 206 行（2082 -> 1876 行）**：抽取藏宝阁商铺 `RpgShopModal.vue` 与关卡战报 `RpgBattleResultModal.vue`，并沉淀修仙专用输入调度引擎 `useRpgInput.ts`。
- **输入引擎逻辑下沉 (Input Engine Composables)**：
  - 沉淀 [`src/composables/useRpgInput.ts`](file:///Users/yuanpinghua/Desktop/Study/wubi-master/src/composables/useRpgInput.ts) 与 [`src/composables/useChaseInput.ts`](file:///Users/yuanpinghua/Desktop/Study/wubi-master/src/composables/useChaseInput.ts)，将底层的键盘事件监听、组字流（Composition）、原生隐藏输入框管理与正统五笔出字逻辑与视图层解耦，实现高内聚的业务逻辑状态机。
- **规范化与类型加固 (Standardization & Bugfix)**：
  - 全面修正各输入视图中非标准的 `inputmode="latin"` 属性为 HTML5 / Vue 严格支持的 `inputmode="text"`，消除所有控制台与 `vue-tsc` 告警；
  - 在 `src/utils/storage.ts` 中建立 `STORAGE_KEYS` 枚举白名单，增加严格校验，防止局部未知键污染 localStorage；
  - 固定 Vite 开发环境端口为 `5175`（`strictPort: true`），确保 E2E 自动化测试套件与本地开发链路稳定对接；
  - **文档与主题规范全面校准**：同步 `README.md`、`docs/ARCHITECTURE.md` 与 `docs/PRODUCT.md`，将历史遗留的“4套”全面校准为真实的 **11 套深浅高对比度无障碍主题**（6 套酷炫暗色 + 5 套简洁明亮），且均达到 WCAG AA 标准。

### ✅ 自动化测试与质量门禁验证 (Quality Assurance)
- **TypeScript 静态检查**：`vue-tsc --noEmit` 0 错误、0 警告；
- **Vitest 单元测试**：7 大测试套件、**99/99 项用例全绿通过**；
- **Playwright 端到端巡检**：`scripts/e2eHarness.ts` 全链路自动化验证 **61/61 项 100% 全部 PASS**（含 UI 8 大核心模块 35 项交互测试 + 13 关修仙全流程真机战斗通关 26 项），支持 `--ui` 与 `--rpg` 参数独立快速回归。

---

## [1.7.2] - 2026-09-16

### 🌟 核心亮点与输入体系重构 (Input Engine Refactor)
- **正统五笔标准出字策略全面确立 (Standard Wubi Commit Specification)**：
  - 确立全应用统一的正统五笔击键规则：**不足 4 码（1~3 码，含一简、二简、三简、3码全码）**在卡槽/输入胶囊中实时回显，**敲击【空格键】确认出字推进**；**满 4 码（4 键全码或4字词组）**打满第 4 键立即瞬间出字推进（0 延时、无需按空格）；
  - **彻底清除全站所有异步 `setTimeout`（90ms/120ms）防抖定时器**：将打字特训 (`TypeEngine.vue`)、长文实战 (`ArticlePractice.vue`)、修仙打怪 (`RpgAdventure.vue`)、极速追逐赛 (`TypingChaseGame.vue`) 全部重构为纯事件驱动与确定性状态机，从架构上根除高速连击（100+ WPM）下的定时器竞争、按键取消覆盖、跨字泄漏与首字永久卡死缺陷。

### 🐛 重点修复与排雷 (Fixed)
- **长文实战输全码出汉字卡死缺陷根治 (Article Practice IME Commit & Capsule Echo Fix)**：
  - 修复输入法选字上屏（如输入 `DEF` 敲空格出 `有`）后，汉字卡在输入胶囊中无法进入后续字词的严重缺陷；
  - 规范输入法组合流（Composition）：过滤组字中间态字母至 `composingText`，并在 `compositionend` 与原生 `input` 同步核销推进、即刻清空输入框与缓存，彻底杜绝汉字留存卡死；
- **RPG 修仙输入卡槽实时回显缺陷修复 (RPG Adventure Input Slots Composing Echo)**：
  - 修复输入法组字过程中 4 位卡槽仅显示第 1 码的缺陷：引入 `activeInputChars` 响应式计算属性，实时动态回显输入法当前组字字母（如 `FGH`）至卡槽；
  - 修复 `onCompositionEnd` 与 `onNativeInput`：同时兼容汉字上屏与纯字母编码上屏，杜绝走火入魔误判；
- **全量自动化测试 100% 满分通过**：
  - 99 项 Vitest 单元测试全部通过；
  - 统一端到端自动化巡检 Harness (`scripts/e2eHarness.ts`) 61 项全量通过（UI 8大核心模块 35 项 + RPG 13关实战 26 项，100% 满分通过）。

---

## [1.7.1] - 2026-09-16

### 🐛 重点修复与排雷 (Fixed)
- **打字特训与RPG闯关输入残留跨字泄漏及首字卡死缺陷根治 (Input Carry-Over & Stuck State Fix)**：
  - **根因分析 1（跨字编码泄漏）**：在 Vue 3 响应式单向流中，当单字命中简码/全码自动出字并清空 reactive `inputBuffer` 时，未在原生 DOM `<input>` 元素上显式调用 `target.value = ''` 与 `inputRef.value.value = ''`。导致下一个字输入时，旧字按键与新按键在原生事件流中累加（例如 `上` 的 `HH` 遗留进 `不` 中形成 `HHG`），造成后续汉字持续报错无法推进；
  - **根因分析 2（高速连击取消导致首字永久卡死）**：当用户以 100+ WPM 高速敲字时，前一个简码的 `autoCommitTimer`（90ms）尚未延时触发，下一个字的按键便已按下。旧逻辑无差别 `clearTimeout` 导致前字提交逻辑被直接腰斩，新按键被错误算作前字的错误拼码，造成“一直卡在第一个字”；
  - **根因分析 3（RPG 物理击键异步残留与焦点丢失）**：RPG 原生隐藏输入框在 `keydown` 未阻止默认动作，导致按键字符在 `onPlayerHit` 同步置空后仍被浏览器原生追加到 DOM；且缺少全局窗口监听，导致失焦后打字无响应。
  - **解决方案与加固**：
    - 在 `TypeEngine.vue`、`RpgAdventure.vue` 与 `ArticlePractice.vue` 建立统一定时器清理器 `clearAutoCommitTimer()`；
    - 引入连击智能顺延机制：若待出字定时器存在且旧缓冲已匹配，用户键入新键时**立即结算并提交当前字**，并将新按键顺延作为下一个字的第 1 码（0 延迟、0 丢键、0 卡顿）；
    - 全面接管 DOM 层面 `<input>` 的 `value = ''` 同步清空，杜绝跨字残留；
    - `RpgAdventure.vue` 拦截 `keydown` 物理字符默认插入动作，改由 `inputKeys` 绝对受控管理，并挂载全局聚焦兜底监听器；
    - `wubiEngine.ts` 升级 `evaluateInput` 引入 `getAllValidCodes`，完整支持一简、二简、键名二简及全码全方位无缝识别。

---

## [1.7.0] - 2026-09-16

### 🌟 核心亮点
- **极致白 (Pure White) 全栈主题适配与 Apple Light 微高程体系重塑**：
  - 彻底打破原版画布与卡片均为 `#ffffff` 造成的“死白无层级”扁平感，升级规范：画布底色 `--bg-primary: #f5f5f7`、卡片背景 `--card-bg: #ffffff`、次级背景 `--bg-tertiary: #e5e5ea`，视觉层次纯净自然；
  - 修复 RPG 战斗核心汉字展示 `.target-character-display` 灰白硬编码导致的白底白字致命不可见 Bug，改为 `var(--text-main)` 配合精致动态光晕，在全主题下深邃醒目；
  - 重构顶部 HUD 藏宝阁与关卡地图按钮 (`.hud-btn`)，消除深石板灰暗斑与低对比度失谐，在浅色主题下呈现高质感苹果轻灰胶囊按钮。
- **全站状态色达到 WCAG AA 无障碍标准 ($\ge$ 4.5:1)**：
  - 成功色 `--success` 升级为深森林绿 `#15803d`（对比度 **4.6:1** 达标）；
  - 警告色 `--warning` 升级为琥珀深棕 `#b45309`（对比度 **4.5:1** 达标）；
  - 次级文字 `--text-muted` 统一优化为 `#636366`（对比度 **5.5:1** 达标），全站字典反查、规则导学、长文实战与虚拟键盘的状态标签彻底攻克对比度盲区。
- **首屏主包 85% 极速瘦身与异步分包架构**：
  - 引入 Vue 3 `defineAsyncComponent` 对次级重型模块（RPG闯关、长文实战、极速追逐赛、规则导学、字典反查、错题本）实施按需懒加载；
  - **首屏入口 JS 压缩体积从 1,438 kB 暴降至 98.21 kB（Gzip 后仅 45.71 kB，体积压缩超 85%）**，消除 Vite 动态导入告警，达成首屏瞬开。
- **URL Hash 路由双向同步体系**：
  - 切换功能 Tab 时无感同步 `window.location.hash`（如 `/#rpg`、`/#article`、`/#lookup`）；
  - 监听 `hashchange` 事件，完美支持浏览器“前进 / 后退”历史导航与页面刷新状态留存；
  - 规范 SEO 别名映射与 `sitemap.xml` 索引结构。
- **统一端到端自动化巡检 Harness (`scripts/e2eHarness.ts`)**：
  - 贯彻“单一测试入口”规范，将全站测试收敛至 `scripts/e2eHarness.ts`，支持 `bun run test:e2e` 一键穿透 UI 8 大核心交互（35项）与 RPG 13 关全实战（26项）；
  - **全量 61 项用例 100% 全部通过**，零报错、零跳过。

### 🐛 重点修复与排雷 (Fixed)
- **RPG 战斗核心汉字白底白字不可见问题**：`.target-character-display` 移除 `#f8fafc` 硬编码，绑定主题文本主色 `var(--text-main)`；
- **RPG 顶部 HUD 藏宝阁暗斑与深灰文字问题**：`.hud-btn` 移除未定义的 `--bg-hover` 回退，绑定 `var(--bg-primary)`、`var(--border-color)` 与 `var(--text-main)`；
- **移动端 iOS 视口防强制缩放加固**：隐藏式虚拟输入框声明 `font-size: 16px`，彻底杜绝 iOS Safari 唤起软键盘时的视口自动畸变放大。

---

## [1.6.0] - 2026-09-16

### 🌟 核心亮点
- **「五笔修仙打怪闯关 (RPG)」全量上线**：
  - 构建完整的十三境界修仙修真体系（练气初悟、筑基稳固、金丹初成、元婴遁地、化神出窍、炼虚合道、合体圆融、大乘登峰、渡劫飞升等）；
  - 题库容量扩充 10 倍，收录 400+ 个五笔核心高频字、各区代表性字根字、一/二/三级简码字及常用战斗词组，彻底告别单调机械；
  - 深度即时战斗（ATB）系统：玩家击键触发飞剑普攻与连续连击加成，Boss 携带独立充能攻击条与 35% 残血狂暴加速机制；
  - 引入「⚡ 飞剑灵气蓄力系统」与「🗡️ 万剑归宗」全屏终极绝技（Tab 键释放，造成 40% 最大生命值巨额伤害并冰冻 Boss 5 秒）；
  - 斩妖掉落奇遇宝箱：胜利结算交互式宝箱，掉落灵石金币、回血还魂丹、护盾符箓及攻击倍率丹等实战道具；
  - 双重二次挑战机制：已通关关卡支持「🎲 乱序破阵（Fisher-Yates 动态洗牌）」与「📖 循序复习」双模式重练，每次闯关皆有全新体验。
- **智能简码防漏字与毫秒级自动跳字引擎**：
  - 彻底打通简码与全码双通道：支持一级单键简码、二级双键简码、三级三键简码及四码全码任意打法；
  - 独创 120ms 智能延时判定时序：当用户停顿达到 120ms 时自动将命中简码的汉字提交上屏并推进到下一字；而若用户继续高速连击输入后续全码，立即清除防抖定时器，既保证简码快速出字，又杜绝全码击键吞字与漏键。
- **全链路 Puppeteer 浏览器端 E2E 自动化测试体系**：
  - 编写并落地 `scripts/e2eAllStagesHarness.ts` 端到端自动化巡检 Harness；
  - 在真实无头浏览器中端到端模拟 1~13 关全量打字打怪流程（含故意按错扣血、大招释放、宝箱开启、乱序复习等）；
  - 13 大关卡 100% 自动化全通，自动生成各关卡通关快照归档至 `e2e_screenshots/`。

### 🐛 重点修复与学术排雷 (Fixed)
- **多键简码（如二级/三级简码）输入卡住无法自动推进下一字问题**：
  - 根因：原 `checkAutoCommit` 存在 `typed.length === 1` 的单键硬编码校验，导致二级简码（如 `木` 对应 `SS`）或三级简码打完后无法触发自动跳转；
  - 方案：重构 `checkAutoCommit`，解绑长度限制，支持所有合法的简码/全码数组比对，并引入 120ms 防抖跳字机制，彻底消除打字卡顿。
- **二次闯关/重复打怪时打第一个字即提前胜利的结算 Bug**：
  - 根因：二次打怪时复用了前一次的 Boss 动态血量状态，导致由于扣血溢出或初始怪物血量判定失真而在第一击直接触发 `monsterHp <= 0` 结算弹窗；
  - 方案：重构胜负判定状态机，关卡彻底通关结算严格与当前关卡目标列表 `activeStageTargets` 是否打完完成强绑定，同时在关卡初始化/重试时深度重置怪物满血与 ATB 计时器，杜绝提前误胜。

### 🚀 新增与改进 (Added & Improved)
- 新增 `src/components/RpgAdventure.vue`：修仙闯关核心战场组件，集成 Canvas 粒子光效、ATB 战斗时序与沉浸式 UI；
- 新增 `src/data/rpgStages.ts`：13 大修仙关卡配置与 400+ 字符题库体系；
- 新增 `src/stores/useRpgStore.ts`：修仙闯关持久化仓库（境界、金币、关卡星级、背包道具）；
- 新增 `scripts/e2eAllStagesHarness.ts`：基于 Puppeteer 的 13 关端到端自动化测试与回归巡检脚本。

---

## [1.5.0] - 2026-09-15

### 🌟 核心亮点
- **常用词组库深度扩容（3,000+ 条高频现代词汇）**：
  - 提取 2,000 双字高频词 + 400 三字词 + 600 四字成语/词组，语料来自高频权威语料库；
  - **100% 纯动态算码**：全量基于 `calculatePhraseCode` 动态推导三代编码与拼音，零体积冗余，主包保持极限轻量（Gzip 仅 ~396KB）；
  - `TypeEngine.vue` 词组特训板块直接接入 3,000 词库，支持海量真实语言环境下的五笔词组盲打训练。
- **引入 Vitest 自动化防回退测试体系**：
  - 建立 3 大专项测试套件（72 个断言用例 100% 通过，耗时 <2s）；
  - **规则防退化**：锁定一级简码 25 字单键、键名字 25 字 4 击、成字字根（五、干、雨、石等）报户头取码，严防“五”等字再次多出第 5 码；
  - **清洗防退化**：锁定业/亚部汉字“业头”、祭字头与登字头区分、足部展开（口+止）、革部展开（廿+十）、Unicode 生僻字符清洗；
  - **算法防退化**：锁定双字/三字/四字/多字词组全套五笔推导规则。

### 🚀 新增与改进 (Added & Improved)
- 新增 `src/data/phraseData.ts`：导出 3,000 高频词汇表及 `getAllDynamicPhrases`、`getPhrasesByLength` 动态生成 API；
- 扩展 `src/data/wordsDict.ts`：`COMMON_PHRASES` 扩容至 3,010+ 词，保持与既有调用完全向下兼容；
- 升级 `src/components/TypeEngine.vue`：词组特训分类升级为「高频词组特训 (3000词)」；
- 新增测试配置 `vitest.config.ts` 与三组自动化测试文件：
  - `tests/wubiRules.test.ts`（43 tests）
  - `tests/wubiDictCorrections.test.ts`（21 tests）
  - `tests/phraseCalculation.test.ts`（8 tests）

---

## [1.4.0] - 2026-09-15

### 🌟 核心亮点
- **业/亚部汉字字根全面纠正**：消除 14 个汉字（晋、亚、业、恶、严、哑、娅、垩、垭、戬、桠、痖、鄑、挜）中原始字典将 O 键字根错标为 `灬`（四点底）的学术顽疾，正确还原为 `业`（业字头/亚字头上部）。
- **IndexedDB 双保险同步机制**：`queryByChar` 改为内存精修优先，新增 `applyDbCorrections` 启动补丁，彻底解决旧 IndexedDB 缓存覆盖字根纠正的问题。

### 🐛 重点修复与学术排雷 (Fixed)

- **业/亚部汉字字根纠正 (`wubiDict.ts`)**：
  - 五笔口诀"火业头，四点米"中 O 键含 `业头`（业字头），而非 `灬`（四点底）；
  - `EXPERT_CORRECTED_CHARS` 新增 `晋`、`亚`、`业` 三字精准字根数据；
  - `normalizeRoots` 新增第 7 条规则：编码含 `OG` 或 `GO` 且字根含 `灬` → 统一纠正为 `业头`；
  - 覆盖 14 个亚/业部汉字，全部验证通过。

- **字根格子显示规范 (`wubiDict.ts`)**：
  - `ROOT_GLYPH_NORMALIZE` 新增 `'业头' → '业'`，MiZiGe 格子显示字形 `业` 而非两字描述 `业头`；
  - `ROOT_NAME_MAP` 新增 `'业': '业字头'`，格子底部标签正确显示"业字头"。

- **IndexedDB 缓存覆盖问题根治 (`wubiDb.ts`)**：
  - `queryByChar` 改为**内存 WUBI_CHAR_MAP 优先**，IndexedDB 缓存仅作生僻字兜底；
  - 新增 `applyDbCorrections()`：以 `corrections_v1` 版本标记在启动时一次性将精修字根写回 IndexedDB，旧用户无需清缓存即可获得修正数据；
  - `App.vue` 在 `requestIdleCallback` 闲时自动触发补丁。

### 🛠️ 架构说明

数据优先级：`EXPERT_CORRECTED_CHARS (精修)` > `normalizeRoots (规范化)` > `IndexedDB 缓存` > `rawDictData 原始数据`

所有 wubiDict.ts 精修数据（110字）全部包含在 rawDictData（28,058字）范围内，建库时通过 `parseRawEntry` 的优先逻辑自动使用精修版本写入 IndexedDB，数据完整同步，无遗漏。

---

## [1.3.0] - 2026-09-15

### 🌟 核心亮点
- **首屏体积暴跌与代码分割 (Code-Splitting)**：将 2.8 万字的全量字典数据 (`wubiFullDictData.ts` ~2.25MB) 彻底解耦为主包外的独立异步分块，首屏初始打包产物 Gzip 压缩后直接从 ~767KB 骤降至 ~379KB，实现首屏秒开。
- **Dexie.js + IndexedDB 客户端持久化大数据库**：采用浏览器内置 IndexedDB 并发建立多重索引 (`char`, `code86`, `code98`, `codeNew`, `pinyinPlain`, `strokes`, `radical`)，后台闲时零阻塞装载 28,058 汉字。
- **智能多模态搜索体系**：
  - **汉字输入**：支持单字拆解与整词分步推导；
  - **五笔编码反查与前缀自动联想**：输入 1~4 位字母（如 `KH`）即可毫秒级列出所有以此编码开头的候选字网格面板；
  - **纯拼音快速反查**：支持不带调拼音（如 `pao`）瞬间反查对应汉字及其三代五笔编码与字根拆解；
  - **交互式候选字直选**：候选字网格支持一键聚焦，联动下方的书法米字格字根拆解大卡片。
- **双层检索架构与 100% 零延迟输入保障**：打字特训、文章实战与极速赛车模式保留 3,500 常用字常驻内存，确保日常高频打字 100% 同步执行、零异步卡顿。

### 🚀 新增功能 (Added)
- 新增 `src/data/wubiDb.ts`：封装 Dexie.js 数据库实例与多模态调度器 (`searchMultiModal`, `queryByExactCode`, `queryByCodePrefix`, `queryByPinyin`)；
- 新增 `src/data/wubiCommonDictData.ts`：精炼提取 3,500 常用字作为同步核心字库；
- 升级 `src/components/WubiLookup.vue`：
  - 接入 `searchMultiModal` 多模态搜索引擎；
  - 新增候选字自适应网格卡片面板，展示汉字、对应版本编码、拼音与简码标签；
  - 新增数据库状态胶囊 (`⚡ IndexedDB 毫秒级字库已就绪`)；
  - 新增快捷范例标签一键体验（五笔、学堂、跑、KH足部前缀、ABV反查、pao拼音、匜疑难字等）。
- 升级 `src/App.vue`：利用 `requestIdleCallback` 闲时异步初始化数据库，实现首屏零性能损耗。

---

## [1.2.0] - 2026-09-15

### 🌟 核心亮点
- **五笔学术合规深度加固**：彻底解决“构字字根”与“末笔交叉识别码”的混淆误标历史顽疾，严正树立“四码字绝无识别码”标准。
- **特殊部首与错拆字地毯式清洗**：逐一校准足部、祭部、登部、鱼部、骨部、革部汉字拆法，全面消除 Ext-B 生僻字豆腐块方框。

### 🐛 重点修复与学术排雷 (Fixed)
- **末笔识别码判定机制重构 (`src/data/wubiDict.ts`, `WubiLookup.vue`)**：
  - 严格限制：**凡拆出 4 个或 4 个以上字根的汉字，各键位均为真实字根，绝不判定识别码**；仅在字根少于 4 个且全码长于字根数时动态计算识别码。
  - 清除了此前包括 `跑`、`经`、`掉`、`轻`、`承`、`罩` 等在内的 **45 个四码字**因字符元数据误赋值产生的虚假识别码。
- **「跑」及足部汉字（111字）错拆修复 (`KHQN`)**：
  - 将 86 版五笔中未设独立单键的“足字旁（`⻊`）”规范展开为两码：**`口` (K 键)** + **`止` (H 键)**；
  - 解决足字旁导致后续 `勹`(Q 键)、`巳`(N 键) 严重错位且多出假识别码的问题。
- **「蔡」及祭字头汉字规范 (`AWFI`)**：
  - 严格区分 34 W 键的“登祭头”两大构件：`登`、`癸`、`橙` 归入“登字头（`癶`）”；`蔡`、`祭`、`察`、`擦` 归入“祭字头（`祭头`）”；
  - 在 `MiZiGe.vue` 中新增高保真「祭字头」原生矢量 SVG 渲染，告别用错误的 `癶` 充数。
- **「刺」及 Ext-B 生僻字符字体缺失修复 (`GMIJ`)**：
  - 识别并清洗原始数据中引入的 Unicode 扩展 B 区冷门字符 `𡭔` (U+21B54)，全面替换为 86 版正统字根 **`小` (I 键)**；
  - 消除 `刺`、`策`、`枣`、`未`、`朱`、`棘` 等汉字在客户端字体缺失导致的空白方框（豆腐块）。
- **常用词组编码校准 (`src/data/wordsDict.ts`)**：
  - 校准 `春暖花开`（`DJAG`，纠正原尾码误写为 J）；
  - 校准 `厚德载物`（`DTFT`，纠正原载字误写为 T）；
  - 校准 `荷花`（`AWAW`，纠正原误写为 S）；
  - 校准 `技术`（`RFSY`，纠正原术字误写为 I）；
  - 校准 `输入法`（`LTIF`）、`互联网`（`GBMQ`）、`中国特色社会主义`（`KLTY`）。

### 🛠️ 架构与工程完善 (Engineering & Docs)
- 新增 `audit_report.md` 项目全面深度技术评估报告；
- 剥离大型 JSON 循环计算，建立全链路 TypeScript 强类型字典与元数据映射表；
- 本地构建性能优化，Vite 8 + Bun 构建耗时稳定在 360ms 级别。

---

## [1.1.0] - 2026-09-15

### 🌟 核心亮点
- **极速追逐赛 (Cyber Chase)**：引入 2.5D 赛车竞技模式，融合输入法防串键机制与多档 AI 航速。
- **拆字法则深度游戏化**：全面改版拆字规则模块，上线“四大原则图解”、“避坑大PK”、“闯关挑战答题器”与“拆字实验室”。

### 🚀 新增功能 (Added)
- **极速追逐赛模块 (`TypingChaseGame.vue`)**：
  - 2.5D 动态双车赛道与实时里程标尺；
  - 30 ~ 120 WPM 四档 AI 速度阶梯与追击间距动态预警；
  - 终点过线烟花动效与专业级打字结算战报面板。
- **拆字法则互动教学系统 (`RuleTutorial.vue`)**：
  - 权威图解“取大优先、兼顾直观、能连不交、能散不连”拆字四项基本原则；
  - “正拆 vs 误拆”避坑对决竞技场，剖析高频易错字底层成因；
  - 5×3 末笔交叉识别码矩阵交互计算器（5 类末笔笔画 × 3 类字型结构）。
- **输入法原生兼容引擎**：
  - 优化输入监听逻辑，全面兼容用户开启系统中文五笔输入法上屏汉字与系统英文键盘直敲五笔编码双模式。

### 🐛 体验与样式修复 (Fixed)
- 解决赛车对战中小车起跑线溢出与赛道装饰边距问题；
- 修复中文输入法打字过程中的按键残留溢出（bleedover）；
- 优化五套主题（暗夜赛博、复古机械、北欧极光、纸墨素雅、黑客终端）在进度条与弹窗中的文字对比度。

---

## [1.0.0] - 2026-09-14

### 🌟 核心亮点
- **五笔学堂 (Wubi Master) 正式版发布**：新一代三代五笔学习、全量汉字反查、长文盲打实战全能平台。

### 🚀 新增功能 (Added)
- **权威三代全量反查大字典 (`WubiLookup.vue`, `wubiDict.ts`)**：
  - 收录 28,058+ 全量汉字；
  - 86 版、98 版、新世纪版三代编码同屏对照与无感切换；
  - 汉字结构（IDS 序列）、总笔画数、部首展示；
  - 任意多字词组整词编码智能推导与分步解析。
- **书法米字格印章体系 (`MiZiGe.vue`)**：
  - 独创纯 CSS/SVG 矢量米字格印章舞台；
  - 字根按笔顺与对应键位独立陈列。
- **多级打字关卡特训引擎 (`TypeEngine.vue`)**：
  - 覆盖字根、一级简码、二级简码、常用 500/1500/3500 字与词组特训；
  - 支持“自动即走 (无需空格)”与“空格出字”两种击键习惯；
  - 智能简码加速气泡提示。
- **无删减长文实战题库 (`ArticlePractice.vue`)**：
  - 收录《陋室铭》、《爱莲说》、《桃花源记》、《岳阳楼记》、《前赤壁赋》、《出师表》、《春》、《背影》、《荷塘月色》等名篇全文；
  - 视口智能平滑自动跟滚动 (Auto-Scroll) 与断点进度记忆；
  - 支持本地文件（`.txt`/`.md`）上传解析与自由粘贴导入。
- **交互式五笔字根大键盘 (`VirtualKeyboard.vue`)**：
  - 五大区位柔和色谱分区与实时击键指法高亮联动；
  - 键盘口诀、主次字根与键名字图谱展示。
- **错题生字本 (`MistakeNotebook.vue`)**：
  - 本地化错字捕获、按键错误回溯、错误频次统计与一键回炉重练。
- **纯代码 Web Audio 键盘音效引擎 (`audio.ts`)**：
  - 原生合成青轴清脆音、红轴线性音与复古打字机声效，零网络延迟。
- **多套高质感主题系统**：
  - 内置暗夜赛博、复古机械、北欧极光、纸墨素雅、黑客终端 5 种沉浸式主题。

---

## 🎯 后续规划 (Roadmap)
- [x] **v1.3.0**：引入 IndexedDB 倒排索引与代码分割，支持“五笔编码反查汉字”、“输入前缀实时自动联想”与“拼音快速反查”（已于 2026-09-15 达成）；
- [ ] **v1.4.0**：扩充常用多字词组大词库至 5,000+ 条；
- [ ] **v1.5.0**：引入字根动态笔顺书写动画与打字速度历史进阶曲线统计。
